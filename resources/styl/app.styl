.reset-link-style
    text-decoration inherit
    &:link,
    &:active,
    &:visited
        color inherit
    &:hover
        text-decoration inherit
.app__display-none
.q-btn-no-hover-bg:hover
    .q-focus-helper
        background transparent !important
    display none
.reset-line-height
    line-height 1 !important
